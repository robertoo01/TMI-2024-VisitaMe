import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TMI-2024-APP',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
